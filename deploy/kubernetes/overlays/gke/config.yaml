apiVersion: v1
kind: ConfigMap
metadata:
  name: inkflow-config
data:
  # Cloud Storage through its S3-compatible XML API (put HMAC keys + S3_ENDPOINT in the Secret:
  # S3_ENDPOINT=https://storage.googleapis.com).
  S3_REGION: auto
  S3_BUCKET: inkflow-prod-files
  S3_FORCE_PATH_STYLE: 'true'
